# ──────────────────────────────────────────────────────────────────────────────
# CVI620 Final Project — Dependency List
# Self-Driving Car Simulation using CNN  (Winter 2026)
#
# Environment tested on: Python 3.9 / macOS
#
# Setup:
#   python3.9 -m venv venv
#   source venv/bin/activate      # macOS / Linux
#   pip install --upgrade pip
#   pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────────────────

# Deep Learning
tensorflow==2.13.0
keras==2.13.0

# Computer Vision
opencv-python==4.8.0.76

# Numerical / Data
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0

# Plotting
matplotlib==3.7.2

# Image utilities (used in test_simulation.py for PIL decoding)
pillow==10.0.0

# Flask + Socket.IO server (for simulator communication)
# NOTE: Exact versions required — newer versions are incompatible with
#       the Udacity simulator's Socket.IO client protocol.
flask==2.3.3
python-socketio==4.6.1
python-engineio==3.14.2
eventlet==0.33.3

# Misc
h5py==3.9.0
